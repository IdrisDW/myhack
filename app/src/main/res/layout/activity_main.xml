<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="16dp">

    <!-- Status message -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Status"
        android:textSize="16sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:textStyle="bold" />

    <!-- Optional heatmap view -->
    <com.example.myhack.uiApp.FootHeatMapView
        android:id="@+id/heatmap_view"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@id/statusText"
        android:layout_marginTop="12dp" />

    <!-- GridLayout for sensor dots (you may hide this if using only heatmap) -->
    <GridLayout
        android:id="@+id/sensorGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/heatmap_view"
        android:layout_marginTop="12dp"
        android:columnCount="3"
        android:rowCount="6"
        android:alignmentMode="alignMargins"
        android:padding="8dp">
        <!-- Add views dynamically from MainActivity.kt -->
    </GridLayout>

</RelativeLayout>
